{
  "name": "@repo/desktop-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsgo --project tsconfig.json --noEmit",
    "typecheck": "tsgo --project tsconfig.json --noEmit",
    "lint": "oxlint \"src/**/*.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.3"
  }
}
